# Copie para frontend/.env.local e preencha com os valores do projeto Supabase
# (Project Settings > API Keys). Use só a chave PÚBLICA (anon ou sb_publishable_...);
# nunca a service_role nem uma sb_secret_...
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
