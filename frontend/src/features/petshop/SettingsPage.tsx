import { EmptyState } from '../../shared/ui/EmptyState'

/** Placeholder até T-08 (dados da loja). */
export function SettingsPage() {
  return (
    <EmptyState
      icon="settings"
      title="Configurações em construção"
      text="Em breve você vai editar o nome, o e-mail e o telefone da sua loja aqui."
    />
  )
}
